import java.util.PriorityQueue;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Map.Entry;
import java.util.Set;

//Functions needed to decode text files to byte array.
public class DecompTree {
	Value top;
	
	public void Decomp(byte[] bytes) {
		int fillerBits = (int)(bytes[0]);
		
		// Reassemble frequency tree.
		byte curByte = bytes[1];
		while(!((char)(curByte)+"").equals("\n")) {
			// TODO: implement word functionality!
			(char)(curByte)
		}
	}
}
